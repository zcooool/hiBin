/**
 * 1. 自带全局变量mui，可直接使用
 */
//初始化变量


//添加事件
var moment = require('moment')
var mui =require('mui')
var view = require('./view')
console.log(moment().format('llll')+'开始执行模块simple的初始化....');
//init();初始化数据
//initEvent();初始化事件






